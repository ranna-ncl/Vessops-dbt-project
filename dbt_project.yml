name: VFM_DBT
version: 1.0.0
config-version: 2
profile: VFM_DBT
model-paths:
  - models
analysis-paths:
  - analyses
test-paths:
  - tests
seed-paths:
  - seeds
macro-paths:
  - macros
snapshot-paths:
  - snapshots
models:
  staging:
    NCL:
      +materialized: view
      +schema: L00_STG
